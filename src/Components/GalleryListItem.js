import React, { useRef } from 'react';

export default function GalleryList({id}) {

    //Artist + Event Name
    //Gallery + link?
    //Location
    //Description
    //List of pics (with one main pic)
    return (
        <li>
            {id}
        </li>
    )
}